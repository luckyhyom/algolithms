### 정렬된 배열을 이진 검색 트리로 변환하기

```cpp
int mid = end / 2; // 배열을 잘라서 인자로 넘길 때에만 가능하다.
int mid = (start + end) / 2; // 배열을 자르지 않고 범위를 지정할 때
```

### Tree는 계층 표현이 가능한 자료구조이지만, BST는 계층에 의미를 두지 않는다. 좌/우 기준으로 정렬된다.
```
    3
  2   4
1       5
```